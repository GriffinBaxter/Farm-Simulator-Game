/**
 * Main CLI package for the Command Line version of the Farm Simulator game. The game is started by running this package's GameEnvironment class, which makes use of some of the Main (GUI) package's classes.
 */
package main_cli;